fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1

# AI provider SDKs — pre-provisioned, used only if a backend spec wires
# into backend/src/llm/. Remove unused ones once the product's AI
# Involvement and provider choice are settled in design/foundations.md.
google-genai>=1.0.0
anthropic>=0.40.0
openai>=1.54.0
